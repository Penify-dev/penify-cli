[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = ">=2.25.0"
gitpython = ">=3.1.0"
tqdm = ">=4.62.0"
python-dotenv = ">=1.0.0"
pytest = ">=7.0.0"
pytest-cov = ">=4.0.0"
coverage = ">=6.0.0"
coverage-badge = ">=1.1.0"

[dev-packages]

[requires]
python_version = "3.13"
